{driver, erl_cache_driver}.

{duration, 3}.
{report_interval, 3}.

{concurrent, 128}.
{mode, max}.

{operations, [{set,  1}, {get, 99}]}.
%{operations, [{set, 10}, {get, 90}]}.
%{operations, [{set, 20}, {get, 80}]}.

%% vim:ft=erlang:
